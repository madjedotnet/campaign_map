{% extends 'base.html.twig' %}

{% block title %}Armies{% endblock %}

{% block body %}
    <h1>Armies</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ army.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ army.name }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ army.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('armies_index') }}">back to list</a>

    <a href="{{ path('armies_edit', {'id': army.id}) }}">edit</a>

    {{ include('armies/_delete_form.html.twig') }}
{% endblock %}
