{% extends 'base.html.twig' %}

{% block title %}WarhammerArmies{% endblock %}

{% block body %}
    <h1>WarhammerArmies</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ warhammer_army.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ warhammer_army.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('warhammer_armies_index') }}">back to list</a>

    <a href="{{ path('warhammer_armies_edit', {'id': warhammer_army.id}) }}">edit</a>

    {{ include('warhammer_armies/_delete_form.html.twig') }}
{% endblock %}
