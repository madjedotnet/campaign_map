{% extends 'base.html.twig' %}

{% block title %}Edit Tiles{% endblock %}

{% block body %}
    <h1>Edit Tiles</h1>

    {{ include('tiles/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tiles_index') }}">back to list</a>

    {{ include('tiles/_delete_form.html.twig') }}
{% endblock %}
